/// <reference types="node" />
/// <reference types="jest" />
import type { ElectronApplication, Frame, Page } from "playwright";
export declare const appPaths: Partial<Record<NodeJS.Platform, string>>;
export declare function itIf(condition: boolean): jest.It;
export declare function describeIf(condition: boolean): jest.Describe;
export declare function start(): Promise<{
    app: ElectronApplication;
    window: Page;
    cleanup: () => Promise<void>;
}>;
export declare function clickWelcomeButton(window: Page): Promise<void>;
/**
 * From the catalog, click the minikube entity and wait for it to connect, returning its frame
 */
export declare function lauchMinikubeClusterFromCatalog(window: Page): Promise<Frame>;
