import type { IComputedValue } from "mobx";
export interface TrayIcon {
    iconPath: string;
    priority: number;
    shouldBeShown: IComputedValue<boolean>;
}
export declare const trayIconInjectionToken: import("@ogre-tools/injectable").InjectionToken<TrayIcon, void>;
