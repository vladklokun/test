export interface MinimalTrayMenuItem {
    id: string;
    parentId: string | null;
    enabled: boolean;
    label?: string;
    click?: () => Promise<void> | void;
    tooltip?: string;
    separator?: boolean;
}
export interface ElectronTray {
    start(): void;
    stop(): void;
    setMenuItems(menuItems: MinimalTrayMenuItem[]): void;
    setIconPath(iconPath: string): void;
}
declare const electronTrayInjectable: import("@ogre-tools/injectable").Injectable<ElectronTray, unknown, void>;
export default electronTrayInjectable;
