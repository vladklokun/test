import type { HelmRepo } from "../../../../common/helm/helm-repo";
import type { AsyncResult } from "../../../../common/utils/async-result";
interface HelmRepositoryFromYaml {
    name: string;
    url: string;
    caFile: string;
    certFile: string;
    insecure_skip_tls_verify: boolean;
    keyFile: string;
    pass_credentials_all: boolean;
    password: string;
    username: string;
}
export interface HelmRepositoriesFromYaml {
    repositories: HelmRepositoryFromYaml[];
}
declare const getActiveHelmRepositoriesInjectable: import("@ogre-tools/injectable").Injectable<() => Promise<AsyncResult<HelmRepo[]>>, unknown, void>;
export default getActiveHelmRepositoriesInjectable;
