import type { JsonObject } from "type-fest";
import type { Cluster } from "../../../common/cluster/cluster";
export interface InstallChartArgs {
    chart: string;
    values: JsonObject;
    name: string;
    namespace: string;
    version: string;
}
declare const installHelmChartInjectable: import("@ogre-tools/injectable").Injectable<(cluster: Cluster, data: InstallChartArgs) => Promise<{
    log: string;
    release: {
        name: string;
        namespace: string;
    };
}>, unknown, void>;
export default installHelmChartInjectable;
