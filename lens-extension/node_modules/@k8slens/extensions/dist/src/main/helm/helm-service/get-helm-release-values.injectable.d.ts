import type { Cluster } from "../../../common/cluster/cluster";
interface GetReleaseValuesArgs {
    cluster: Cluster;
    namespace: string;
    all: boolean;
}
declare const getHelmReleaseValuesInjectable: import("@ogre-tools/injectable").Injectable<(releaseName: string, { cluster, namespace, all }: GetReleaseValuesArgs) => Promise<string>, unknown, void>;
export default getHelmReleaseValuesInjectable;
