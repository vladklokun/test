/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import type WebSocket from "ws";
import type { ProxyApiRequestArgs } from "../types";
import type { ClusterManager } from "../../../cluster-manager";
import type { Cluster } from "../../../../common/cluster/cluster";
import type { ClusterId } from "../../../../common/cluster-types";
interface Dependencies {
    authenticateRequest: (clusterId: ClusterId, tabId: string, shellToken: string | undefined) => boolean;
    createShellSession: (args: {
        webSocket: WebSocket;
        cluster: Cluster;
        tabId: string;
        nodeName?: string;
    }) => {
        open: () => Promise<void>;
    };
    clusterManager: ClusterManager;
}
export declare const shellApiRequest: ({ createShellSession, authenticateRequest, clusterManager }: Dependencies) => ({ req, socket, head }: ProxyApiRequestArgs) => void;
export {};
