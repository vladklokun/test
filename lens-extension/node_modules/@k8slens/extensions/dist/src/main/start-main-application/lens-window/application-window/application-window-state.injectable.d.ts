import windowStateKeeper from "electron-window-state";
interface WindowStateConfiguration {
    id: string;
    defaultHeight: number;
    defaultWidth: number;
}
declare const applicationWindowStateInjectable: import("@ogre-tools/injectable").Injectable<windowStateKeeper.State, unknown, WindowStateConfiguration>;
export default applicationWindowStateInjectable;
