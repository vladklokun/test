import { LocalShellSession } from "./local-shell-session";
import type { Cluster } from "../../../common/cluster/cluster";
import type WebSocket from "ws";
interface InstantiationParameter {
    webSocket: WebSocket;
    cluster: Cluster;
    tabId: string;
}
declare const localShellSessionInjectable: import("@ogre-tools/injectable").Injectable<LocalShellSession, unknown, InstantiationParameter>;
export default localShellSessionInjectable;
