import type { Cluster } from "../../common/cluster/cluster";
import type WebSocket from "ws";
interface Args {
    webSocket: WebSocket;
    cluster: Cluster;
    tabId: string;
    nodeName?: string;
}
declare const createShellSessionInjectable: import("@ogre-tools/injectable").Injectable<({ nodeName, ...rest }: Args) => import(".").LocalShellSession | import(".").NodeShellSession, unknown, void>;
export default createShellSessionInjectable;
