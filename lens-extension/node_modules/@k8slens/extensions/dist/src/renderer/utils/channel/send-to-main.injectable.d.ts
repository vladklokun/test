import type { JsonValue } from "type-fest";
declare const sendToMainInjectable: import("@ogre-tools/injectable").Injectable<(<T>(channelId: string, message: JsonValue extends T ? T : never) => void), unknown, void>;
export default sendToMainInjectable;
