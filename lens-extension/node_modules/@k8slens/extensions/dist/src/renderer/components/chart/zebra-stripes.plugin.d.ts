/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import type ChartJS from "chart.js";
import type { Moment } from "moment";
import type { PluginServiceRegistrationOptions } from "chart.js";
export interface ZebraStripesOptions {
    stripeColor: string;
    interval: number;
}
export declare class ZebraStripesPlugin implements PluginServiceRegistrationOptions {
    updated: Moment | null;
    options: ZebraStripesOptions;
    constructor(options?: Partial<ZebraStripesOptions>);
    getOptions(chart: ChartJS): ZebraStripesOptions | undefined;
    getLastUpdate(chart: ChartJS): Moment;
    getStripesElem(chart: ChartJS): HTMLElement | null | undefined;
    removeStripesElem(chart: ChartJS): void;
    updateOptions(chart: ChartJS): void;
    getStripeMinutes(): 0 | 10;
    renderStripes(chart: ChartJS): void;
    afterInit(chart: ChartJS): void;
    afterUpdate(chart: ChartJS): void;
    resize(chart: ChartJS): void;
    afterDatasetUpdate(chart: ChartJS): void;
}
