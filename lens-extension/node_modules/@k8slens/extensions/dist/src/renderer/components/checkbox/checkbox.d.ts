/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import "./checkbox.scss";
import React from "react";
import type { SingleOrMany } from "../../utils";
export interface CheckboxProps {
    className?: string;
    label?: React.ReactNode;
    inline?: boolean;
    disabled?: boolean;
    value?: boolean;
    onChange?(value: boolean, evt: React.ChangeEvent<HTMLInputElement>): void;
    children?: SingleOrMany<React.ReactChild | React.ReactFragment>;
}
export declare function Checkbox({ label, inline, className, value, children, onChange, disabled, ...inputProps }: CheckboxProps): JSX.Element;
