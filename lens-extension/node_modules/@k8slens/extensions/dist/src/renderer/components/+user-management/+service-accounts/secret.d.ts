/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import "./secret.scss";
import React from "react";
import type { Secret } from "../../../../common/k8s-api/endpoints/secret.api";
export interface ServiceAccountsSecretProps {
    secret: Secret | string;
}
interface State {
    showToken: boolean;
}
interface RenderRowArgs {
    name: string;
    value: React.ReactNode;
}
export declare class ServiceAccountsSecret extends React.Component<ServiceAccountsSecretProps, State> {
    state: State;
    renderSecretValue(secret: Secret): JSX.Element;
    renderRow({ name, value }: RenderRowArgs): JSX.Element;
    render(): JSX.Element;
}
export {};
