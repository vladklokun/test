/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import type { ImgHTMLAttributes, MouseEventHandler } from "react";
import React from "react";
import type { SingleOrMany } from "../../utils";
export interface AvatarProps {
    title: string;
    colorHash?: string;
    size?: number;
    src?: string;
    background?: string;
    variant?: "circle" | "rounded" | "square";
    imgProps?: ImgHTMLAttributes<HTMLImageElement>;
    disabled?: boolean;
    children?: SingleOrMany<React.ReactNode>;
    className?: string;
    id?: string;
    onClick?: MouseEventHandler<HTMLDivElement>;
    "data-testid"?: string;
}
export declare const Avatar: ({ title, variant, size, colorHash, children, background, imgProps, src, className, disabled, id, onClick, "data-testid": dataTestId, }: AvatarProps) => JSX.Element;
