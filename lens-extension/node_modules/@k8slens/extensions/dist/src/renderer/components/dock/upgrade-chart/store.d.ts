/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import type { TabId } from "../dock/store";
import type { DockTabStoreDependencies } from "../dock-tab-store/dock-tab.store";
import { DockTabStore } from "../dock-tab-store/dock-tab.store";
export interface IChartUpgradeData {
    releaseName: string;
    releaseNamespace: string;
}
export interface UpgradeChartTabStoreDependencies extends DockTabStoreDependencies {
    valuesStore: DockTabStore<string>;
}
export declare class UpgradeChartTabStore extends DockTabStore<IChartUpgradeData> {
    protected readonly dependencies: UpgradeChartTabStoreDependencies;
    private get releaseNameReverseLookup();
    get values(): DockTabStore<string>;
    constructor(dependencies: UpgradeChartTabStoreDependencies);
    reloadValues(tabId: TabId): Promise<void>;
    getTabIdByRelease(releaseName: string): string | undefined;
}
