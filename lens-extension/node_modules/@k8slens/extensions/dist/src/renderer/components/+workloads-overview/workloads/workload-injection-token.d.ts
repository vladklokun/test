import type { IComputedValue } from "mobx";
export interface Workload {
    resourceName: string;
    open: () => void;
    amountOfItems: IComputedValue<number>;
    status: IComputedValue<Partial<Record<string, number>>>;
    title: string;
    orderNumber: number;
}
export declare const workloadInjectionToken: import("@ogre-tools/injectable").InjectionToken<Workload, void>;
