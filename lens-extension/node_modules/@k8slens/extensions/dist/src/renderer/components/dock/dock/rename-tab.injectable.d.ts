import type { TabId } from "./store";
declare const renameTabInjectable: import("@ogre-tools/injectable").Injectable<(tabId: TabId, title: string) => void, unknown, void>;
export default renameTabInjectable;
