/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import React from "react";
import type { UseTableOptions } from "react-table";
export declare type SearchFilter<T> = (item: T) => string | number;
export interface ListProps<T> extends UseTableOptions<any> {
    items: T[];
    filters: SearchFilter<T>[];
    title?: React.ReactNode;
}
export declare function List<T>({ columns, data, title, items, filters }: ListProps<T>): JSX.Element;
