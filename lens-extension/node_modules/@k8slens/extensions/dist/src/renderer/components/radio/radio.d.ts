/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import "./radio.scss";
import React from "react";
import type { SingleOrMany } from "../../utils";
export interface RadioGroupProps<T> {
    className?: string;
    value?: T;
    asButtons?: boolean;
    disabled?: boolean;
    onChange: (value: T) => void;
    children: SingleOrMany<React.ReactElement<RadioProps<T>>>;
}
export declare function RadioGroup<T>({ value, asButtons, disabled, onChange, className, children, }: RadioGroupProps<T>): JSX.Element;
export interface RadioProps<T> {
    className?: string;
    label: React.ReactNode;
    value: T;
    disabled?: boolean;
}
export declare function Radio<T>({ className, label, value, disabled, }: RadioProps<T>): JSX.Element;
