/// <reference types="node" />
import type { ExtensionDiscovery } from "../../../../../extensions/extension-discovery/extension-discovery";
import type { LensExtensionId, LensExtensionManifest } from "../../../../../extensions/lens-extension";
import type { InstallRequest } from "../install-request";
export interface InstallRequestValidated {
    fileName: string;
    data: Buffer;
    id: LensExtensionId;
    manifest: LensExtensionManifest;
    tempFile: string;
}
interface Dependencies {
    extensionDiscovery: ExtensionDiscovery;
}
export declare const createTempFilesAndValidate: ({ extensionDiscovery }: Dependencies) => ({ fileName, dataP, }: InstallRequest) => Promise<InstallRequestValidated | null>;
export {};
