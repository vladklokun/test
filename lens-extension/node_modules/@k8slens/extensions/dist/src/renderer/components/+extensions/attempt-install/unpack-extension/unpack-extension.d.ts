/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import type { InstallRequestValidated } from "../create-temp-files-and-validate/create-temp-files-and-validate";
import type { Disposer } from "../../../../../common/utils";
import type { ExtensionLoader } from "../../../../../extensions/extension-loader";
import type { ExtensionInstallationStateStore } from "../../../../../extensions/extension-installation-state-store/extension-installation-state-store";
interface Dependencies {
    extensionLoader: ExtensionLoader;
    getExtensionDestFolder: (name: string) => string;
    extensionInstallationStateStore: ExtensionInstallationStateStore;
}
export declare const unpackExtension: ({ extensionLoader, getExtensionDestFolder, extensionInstallationStateStore, }: Dependencies) => (request: InstallRequestValidated, disposeDownloading?: Disposer) => Promise<void>;
export {};
