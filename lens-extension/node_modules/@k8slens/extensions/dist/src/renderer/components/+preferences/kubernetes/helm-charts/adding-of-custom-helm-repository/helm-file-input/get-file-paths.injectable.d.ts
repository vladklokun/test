import type { FileFilter } from "electron";
declare const getFilePathsInjectable: import("@ogre-tools/injectable").Injectable<(fileFilter: FileFilter) => Promise<{
    canceled: boolean;
    filePaths: string[];
}>, unknown, void>;
export default getFilePathsInjectable;
