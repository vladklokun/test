/**
 * Copyright (c) OpenLens Authors. All rights reserved.
 * Licensed under MIT License. See LICENSE in root directory for more information.
 */
import type { FileFilter, OpenDialogOptions } from "electron";
import React from "react";
export interface PathPickOpts {
    label: string;
    onPick?: (paths: string[]) => any;
    onCancel?: () => any;
    defaultPath?: string;
    buttonLabel?: string;
    filters?: FileFilter[];
    properties?: OpenDialogOptions["properties"];
    securityScopedBookmarks?: boolean;
}
export interface PathPickerProps extends PathPickOpts {
    className?: string;
    disabled?: boolean;
}
export declare class PathPicker extends React.Component<PathPickerProps> {
    static pick(opts: PathPickOpts): Promise<void>;
    onClick(): Promise<void>;
    render(): JSX.Element;
}
